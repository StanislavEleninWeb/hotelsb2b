# Hoist nothing by default so packages must declare their own deps (catches
# phantom dependencies that would break the Docker production builds).
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
